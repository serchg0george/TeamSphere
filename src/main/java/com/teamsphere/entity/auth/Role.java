package com.teamsphere.entity.auth;

/**
 * Enumeration of user roles in the system.
 * Defines access levels for different user types.
 */
public enum Role {
    /**
     * Administrator role with full system access.
     */
    ROLE_ADMIN,

    /**
     * Regular user role with limited access.
     */
    ROLE_USER
}
